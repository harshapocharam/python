print('hello, world!')
print(1+2)
print(8*3)
print()
print("the end", "or is it?", "keep watching",3)
#========================================================================
# literal - having value of some type like int, string, boolean etc
#function - A name block of code that we can call by using its name. 
#           we can write our own function or we can use build in python.
#argument - A value passed to function in order to work with. This may be orguments of 1 or more
#           arguments appear in parentheses ofter the function name. if no orgumnets we can still have parantheses.
#function call - using function name we can call a function. when we call a function we have to provide
#                arguments that call expected.
#
